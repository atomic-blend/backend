meta {
  name: /getAll
  type: http
  seq: 2
}

get {
  url: {{API_URL}}/tasks/?page=1&size=10
  body: none
  auth: inherit
}

params:query {
  page: 1
  size: 10
}

script:pre-request {
  bru.runRequest("/auth/login")
}

settings {
  encodeUrl: true
  timeout: 0
}
