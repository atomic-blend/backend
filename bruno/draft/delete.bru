meta {
  name: /delete
  type: http
  seq: 5
}

delete {
  url: {{API_URL}}/mail/draft/{{DRAFT_ID}}
  body: none
  auth: inherit
}

script:pre-request {
  bru.runRequest("/auth/login")
}

settings {
  encodeUrl: true
  timeout: 0
}
